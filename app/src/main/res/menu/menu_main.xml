<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/tools"
    xmlns:appcompat="http://schemas.android.com/apk/res-auto">

    <group android:id="@+id/main_group">
        <item
            android:id="@+id/action_logout"
            android:icon="@drawable/ic_person_24dp_primarylight"
            android:title="@string/logout"
            appcompat:showAsAction="withText" />


    </group>

    <group android:id="@+id/overview_group">

        <item
            android:id="@+id/action_search"
            android:icon="@android:drawable/ic_menu_search"
            android:title="@string/menu_search"
            appcompat:actionViewClass="android.support.v7.widget.SearchView"
            appcompat:showAsAction="ifRoom" />

        <item
            android:id="@+id/action_sortby"
            android:icon="@drawable/ic_sort_24dp_white"
            android:title="@string/SortBy"
            appcompat:showAsAction="ifRoom" />

    </group>


</menu>